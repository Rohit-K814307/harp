import torch
import torch.nn as nn



class HARPEncoder(nn.Module):

    def __init__(self, encoding_dim, numeric_dim, dgns_dim, prcdr_dim, dgns_vocab_size, prcdr_vocab_size):
        super().__init__()

        # define the embedding for dgns + prcdr



        # define layernorm for numerical features


        # 

        pass

